import React, { Fragment } from 'react';
import { Children_Node } from '../../../types/general_types';

const Airtime_View_Modals = ({ children }: Children_Node) => {
  return (
    <Fragment>
      {children}

      <Fragment />

      <Fragment />
    </Fragment>
  );
};

export default Airtime_View_Modals;
