import { Fragment } from 'react';
import { children_type } from '@/types/general_types/general_types';

const Power_View_Modals = ({ children }: children_type) => {
  return (
    <Fragment>
      {children}

      <Fragment />

      <Fragment />
    </Fragment>
  );
};

export default Power_View_Modals;
