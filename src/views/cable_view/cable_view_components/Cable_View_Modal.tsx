import React, { Fragment } from 'react';
import { children_type } from '@/types/general_types/general_types';


const Cable_View_Modals = ({ children }: children_type) => {
  return (
    <Fragment>
      {children}

      <Fragment />

      <Fragment />
    </Fragment>
  );
};

export default Cable_View_Modals;
